遇到问题：
1. toolresult的content块格式适配问题，启发：不同层参数传递时优先做好规范和约定！
2. bash中keyboardinterrupt中断问题，解决：系统提示词提示，工具层兜底
3. 工具权限测试：
4. log的设计问题：太长，可读性问题，
5. 做好文档记录：不完整的问题（在调用该模型前做好记录）
6. 工具调用失败 
   1. 做好参数校验与（模型层）
   2. 做好参数适配（工具层）
   3. 做好参数权限检查(preusetool层（policy层）)
   4. 对可重试命令做退避重试，重试多次进入工具熔断
   5. 给模型返回完整的工具调用错误信息
7. bug:turn嵌套函数内部压缩文件，会将内部的history引用指向新的history，但是外层的history还是原来的history，导致压缩的对话丢失。解决：使用切片赋值。启发：嵌套函数内部修改可变对象参数记得不能直接指向新可变对象而是要用切片原地更新。
8. 